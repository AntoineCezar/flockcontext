============
Installation
============

At the command line::

    $ pip install flockcontext

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flockcontext
    $ pip install flockcontext
